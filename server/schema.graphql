type Query {
  company(id: ID!): Company
  job(id: ID!): Job
  jobs: [Job]
  companies: [Company]
}

type Company {
  id: ID!
  name: String
  description: String
  jobs: [Job]
}

type Job {
  id: ID!
  company: Company
  title: String
  description: String
}
